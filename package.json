{
  "name": "escandir",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:libs": "npm run build -w @escandir/engine && npm run build -w @escandir/lexicon",
    "test": "npm run build:libs && npm run test --workspaces --if-present",
    "typecheck": "npm run build:libs && npm run typecheck --workspaces --if-present",
    "build": "npm run build:libs && npm run build -w @escandir/editor",
    "dev": "npm run dev -w @escandir/editor"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
